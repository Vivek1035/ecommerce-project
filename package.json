{
  "private": true,
  "scripts": {
    "dev:frontend": "npm --prefix ecommerce-project-frontend run dev",
    "dev:backend": "npm --prefix ecommerce-backend run dev",

    "build:frontend": "npm --prefix ecommerce-project-frontend run build",
    "build:backend": "npm --prefix ecommerce-backend run zip",

    "build": "run-s build:frontend build:backend"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
